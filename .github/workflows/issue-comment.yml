name: Gif Comment
on:
  issues:
    types: [opened]

jobs:
  comment:
    if: github.event_name == 'issues'
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: echo "Hello World!"
#     steps:
#       - name: Add funny comment
#         uses: Marta83/funny-comment-action@v2.1
#         id: FunnyComment
#         with:
#           GITHUB_TOKEN: s{{ secrets.PERSONAL_TOKEN }}
#           GIPHY_TOKEN: s{{ secrets.GIPHY_TOKEN }}
#           comment_type: 'gif'
